# This rule is not used by the project redis itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the rediscluster.redis.ejlabs.in.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/managed-by: kustomize
  name: rediscluster-editor-role
rules:
- apiGroups:
  - rediscluster.redis.ejlabs.in
  resources:
  - redisclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - rediscluster.redis.ejlabs.in
  resources:
  - redisclusters/status
  verbs:
  - get
